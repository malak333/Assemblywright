# Developer Build Test and Phase Closeout

The accepted requirement is a usable, supervised Mac-to-Windows implementation and
validation loop. `DESIGN.md`, `docs/safety-rules.md`, and `docs/developer-build.md`
record the owner's deferred production-hardening scope. This phase preserves the
production schema and installed service. Its publication checkout excludes the
unfinished production control, VM, and active-executor drafts.

## Workflow and coverage matrix

The `unit-testing-test-generate` workflow was applied by inspecting state changes,
file operations, queue boundaries, and the Swift HTTP client, then adding focused
success, rejection, boundary, recovery, and error tests. The `e2e-testing` workflow
uses this product's native boundaries. There is no browser surface, so Playwright,
cross-browser matrices, and browser visual baselines are not applicable.

| Boundary | Cases | Test entry point |
| --- | --- | --- |
| Rust state | SQL write failure preserves memory state; failed Emergency/clear writes preserve the volatile latch; unconfirmed termination dominates cancellation; invalid/oversized requests; exact 100-feature capacity and duplicate replay; unauthorized mutation; emergency clear does not start | `cargo test -p assemblywright-master --bin assemblywright-developer` |
| Rust files | Local-only model URL admission; idempotent prepared edits; owner edit during generation; omitted existing files; relative paths; malformed/duplicate/empty/maximum change sets; context exclusions and budget | Same Rust unit suite |
| Swift client | Authenticated status; stale response after a command; rejected command; malformed/wrong-mode/unauthorized/offline response; invalid/missing configuration; observer cancellation | `swift test --disable-sandbox --package-path apps/mac --filter DeveloperRunnerClientTests` |
| Native HTTP/files/processes | Start/Stop/Emergency/clear/Resume; actual file writes and validation; paused checkpoint after restart; no rewrite or model recall on resume; auto-run on/off; malformed generation; failed validation and repair; fixed starting queue frontier; ordinary `basic capabilities` planning prose through approval and enqueue | `python3 scripts/developer-runner-e2e.py --binary target/debug/assemblywright-developer` and `python3 scripts/developer-runner-planning-e2e.py --binary target/debug/assemblywright-developer` |
| Windows process lifetime | Validation parent and descendant exit after Stop, Emergency Pause, and abrupt runner death; paused applied checkpoint resumes after restart | Same native E2E, with Windows-only runner-crash scenario |
| Live Swift-to-Windows | Real app model reads and decodes the running Windows queue | Opt-in `DeveloperRunnerTests`, using `ASSEMBLYWRIGHT_DEVELOPER_LIVE_CONFIG` |
| Live local model | Three features generated by local Qwen and validated on Windows, with 10 then 20 then 20 tests passing | Earlier demonstration in `developer-build.md` |
| Developer GitHub publication | Candidate/base/tree binding; URL/base/policy rejection; required-check app and head identity; cancellation; ambiguous-effect reconciliation; exact merged-base verification; historical local-only state | Rust Developer binary tests plus `scripts/developer-runner-publication-e2e.py` on Mac and Windows |
| Developer GitHub setup | Invalid/expired/masked authentication; stale/busy actions; device challenge and cancellation; pagination; empty-repository branch truth; collision; ambiguous creation and restart; immutable repository ID; strict Swift acknowledgements | Rust Developer binary tests, `scripts/developer-runner-github-setup-e2e.py`, and `DeveloperGitHubSetupTests` |
| Developer Auto AI repair state/UI | Default-off migration; atomic revision-bound settings; `1...100` limit; per-feature snapshot; shared manual/automatic count; evidence capacity; lifecycle/epoch cancellation; strict Swift acknowledgement, conflict refresh, input, accessibility, progress and recovery presentation | Rust Developer binary tests plus `DeveloperRunnerTests`, `DeveloperRunnerClientTests`, and `DeveloperRepairEscalationTests` |
| Developer Auto AI repair process | Three ordinary repairs before automatic escalation; source/test/configuration writes; immutable validation-command string; review rejection; configured and absolute caps; app-independent polling; operational hold; disable/Stop late results; pre-effect restart; validation/review quarantine; project chat plus manual escalation on the shared per-feature cap; automatic stop at the shared cap without repair-model or reviewer calls; owner-corrected bytes with validation-only Resume, fresh validation, and one independent review; validation handle release before cleanup; Windows-only delayed-descendant reap sentinel | `python3 -B scripts/developer-runner-auto-repair-e2e.py --binary target/debug/assemblywright-developer` |

The Swift transport fixture substitutes only HTTP delivery; it exercises the real
client model, decoding, state handling, and error reporting. The native E2E fixture
substitutes only the model response and uses real sockets, SQLite, files, commands,
and process lifecycle. Temporary test roots do not touch the owner's live queue.
Coverage is scenario-based; no numerical line/branch coverage percentage is claimed.
Launcher build/reconnect is exercised on the owner's actual SSH/model setup.

## CI and closeout evidence

`release-local.sh` runs Rust unit/integration tests and the full Swift package.
The master integration test `developer_workflow_e2e` invokes the native developer
E2E; the required Windows distributed workflow runs that same integration test
against its native `.exe` through `cargo test -p assemblywright-master`. No new
optional CI lane is needed. Both required hosted gates must pass before merge.

Documentation and knowledge-base updates accompany this phase. Independent review
found queue-frontier, interrupted-write, and validation-process lifecycle gaps;
the closeout addresses them with corresponding native tests. Final independent
review approved the phase with no remaining in-scope findings after rerunning the
strict Clippy command and whitespace check. Hosted gate results are recorded on
the publication pull request.

A second review added failure handling for process spawn cleanup/polling, a
volatile emergency latch that cannot be cleared by a failed SQLite write, and
local-only model URL validation. These are developer correctness fixes. The
review's proposed production append-only audit and automatic log-redaction layer
were not adopted: the accepted developer scope intentionally retains owner-local
validation logs and a durable current-state snapshot. Those production controls
remain deferred by the owner's instruction; logs are not published by the app.

The isolated phase passed 13 Rust unit tests on Mac, 14 on Windows (including
a real kernel-query failure), and the native process E2E on both hosts. The Windows E2E verified the validation parent and descendant
exited after abrupt runner death; the reported Stop time was under 0.2 seconds.
Six deterministic Swift client tests (including four error-response cases) passed,
and the full Swift package passed 205 tests across seven suites, with the opt-in
live test skipped when no connection configuration was supplied. Both hosts passed
strict Clippy for the developer binary. Final whole-repository and hosted results
are tracked separately from these focused checks.

The earlier broad local run was interrupted by macOS test startup delays. That
partial run is not a full gate pass. Native visual UI automation also failed when
the computer-use bridge closed its pipe; live HTTP client evidence is not a visual
UI test. Signed production installation and protected production execution remain
outside this supervised phase. Developer GitHub publication is the accepted
Windows-owned extension documented in `developer-github-publication-design.md`;
live remote effects and hosted checks remain separate from local fixture evidence.

## Developer GitHub publication and onboarding continuation

The September 8 implementation used the same scenario-based unit workflow and native
E2E boundary. Its publication fixture used actual Git repositories and runner
processes with a controlled GitHub CLI; its setup fixture exercised authentication,
device-code cancellation, repository discovery, durable creation effects, and
observation-only recovery. Swift tests covered strict decoding and revision-bound
acknowledgements. Browser matrices do not apply to the native Swift owner surface;
GitHub's external device page is opened as a fixed trusted sink and is not an
Assemblywright browser UI.

Retained September 8 logs report the focused Developer, Swift, installer, and native
Mac/Windows tests passing. The broad local gate remained red because a separate Mac
relay test returned `UnexpectedEof` while reading a frame prefix. Those results are
historical recovery evidence. The restored source requires fresh focused tests,
native Windows coverage, independent high-risk review, the complete canonical gate,
and exact-commit hosted checks before this new restoration is closed.

## Project chat, planning, review, and escalation continuation

The continuation applies the same unit workflow to exact chat/model attribution,
planning state transitions, attachment bounds, review bindings, proposal lifecycle,
cumulative baselines, cancellation, stale bytes, migration, and recovery. Its native
E2E uses Rust/Swift clients, loopback HTTP, real files, Windows processes, SQLite,
and fixture model/reviewer subprocesses. The `developer_workflow_e2e` Cargo wrapper
runs all twelve current scripts on both required CI hosts. Browser E2E and cross-browser
matrices do not apply because the owner surface is a native Swift application.

Historical focused runs before the 12:50-12:56 owner interaction passed 41 Rust tests,
244 Swift tests, and seven Windows developer process E2Es. The current isolated
candidate at that historical checkpoint separately passed 44 Rust tests on Mac, the same 44 tests on native
Windows, 235 Swift tests, strict developer Clippy, and all seven native Windows
process E2Es. The Cargo wrapper also ran all seven process E2Es locally, including
new validation-failure and reviewer-rejection cases that preserve the auto-run
frontier across restart. The owner screenshots then
confirmed the repaired sheet's pinned Close and action buttons. The owner applied
proposal 4, but its 24-test validation failed two tests: a hidden/unrealized Tk
window height assertion observed height 1, and a shallow/brittle widget-tree search
could not find the nested Input card. Codex review did not run and the feature did
not advance. These failures are current acceptance evidence and keep the GUI feature
open; earlier focused passes do not override them. This is owner-observed native UI
evidence, not machine-automated screenshot regression coverage.

The continuation also covers two fail-closed recovery regressions. If committing an
Emergency Pause fails, the runner retains its volatile emergency latch and rejects
new work rather than presenting a durable acknowledgement. Completed chat and
planning provider responses are retained in memory when their completion write
fails. While that runner process remains alive, status GET polling exposes
`recovery_pending` or `planning_recovery_pending` and retries the exact completion
without reexecuting the provider. A process restart cannot recover that in-memory
response: startup records the pending chat or planning call as interrupted and a
new owner retry may call the provider again.

This evidence satisfies documentation and knowledge capture for the observed state.
The first complete local gate passed at 17:31 UTC, before the latest recovery changes.
The final complete local gate passed at 18:17 UTC on 2026-09-07, including the
recovery and CLI-compatibility corrections. Exact-commit hosted gates remain a
required publication check. A timing-sensitive existing Swift process fixture was
replaced with direct TERM-delivery and process-reaping evidence; the full 235-test
Swift suite passed afterward. Signing, notarization, protected production deployment,
and browser testing are outside this developer-build slice.

The first hosted Windows run caught a grounding fixture that passed a raw temporary
path, although production canonicalizes project roots before traversal. The fixture
now canonicalizes that root, preserving containment checks when Windows TEMP uses
a short-path alias. Native Windows A/B validation with distinct 8.3 TEMP/TMP paths
reproduced the old failure and passed all 44 tests with the corrected fixture.
Independent review approved the test-only correction; the final commit still requires both hosted gates. This
fixture change does not alter the deployed developer runtime.

The developer Mac bundle and Windows runner were rebuilt and the supervised
connection restarted. Authenticated status returned idle with five retained queue
items; their IDs, feature statuses, and checkpoints were unchanged. Both temperature
GUI/test file hashes matched the pre-update snapshot. During the observation interval
the proposal metadata changed from escalation 5 unavailable to escalation 6 ready;
therefore this is not evidence of an entirely unchanged queue snapshot. No repair
approval was submitted by this closeout. Production `AssemblywrightMaster` runtime
inputs were unchanged and its service was not redeployed. The Mac bundle uses an
ad-hoc development signature, not a signed/notarized production installation.

The native catalog probe passed with both installed CLIs and observed zero outgoing
tools: Mac Codex 0.153.4 and Windows Codex 0.148. The Windows compatibility profile
omits four flags that its CLI does not define; all supported tool-disabling settings
remain explicit. The Windows probe captured one loopback-fixture request and made no
real model call. Rerun the catalog probe whenever either configured CLI is upgraded.

## Planning disclosure false-positive closeout

The September 13 planning failure was caused by treating every occurrence of
`Basic ` as credential material, including ordinary requirements such as `basic
arithmetic`. Basic authentication is now recognized only when a case-insensitive
scheme is followed by padded or unpadded Base64 bytes that decode to a value
containing the required `:` credential separator. Credential-shaped provider text
is redacted before response hashing and durable planning-state persistence.

The `assemblywright-developer` binary suite passed 130 tests. Focused unit coverage
binds ordinary prose, malformed and non-credential Base64, mixed-case and whitespace
schemes, short and unpadded credentials, punctuation, multiple credentials,
redaction, persisted sanitized text, and its output digest. The native planning E2E
carried `basic capabilities` through clarification, understanding, design approval,
implementation approval, enqueue, repair, and review. Browser and Playwright matrices
do not apply because this boundary is Rust HTTP/process/file behavior presented by a
native Swift application.

The Developer runner and Mac development bundle were rebuilt, remained connected to
`MIKE-PC`, and all eight live `DeveloperRunnerTests` passed. Retrying the owner-visible
failed planning session advanced it from revision 8 with an error to revision 10 at
the understanding stage; the accepted summary retained `Provide basic arithmetic`.
An independent high-risk review approved the final behavior and coverage.

The canonical local release reached workspace Clippy successfully but remains red:
`supervised_agent_uds_requires_identity_and_bearer_and_persists_exact_cursor` failed
while reading a relay frame prefix with `UnexpectedEof`, and the isolated rerun
reproduced the same pre-existing failure. No changed file in this phase touches the
relay. Exact-commit hosted gates therefore remain required before merge. This proof
does not claim signed/notarized production installation or automated native visual
layout coverage; the native UI automation pipe was unavailable during observation.

## Developer Auto AI repair implementation evidence

The implementation follows the same unit-first and native `e2e-testing` workflows.
The real Developer binary owns policy persistence, queue-v11 backup-first migration,
feature limit snapshots, a shared lifetime escalation cap, bounded evidence,
automatic proposal authorization, file application, validation, independent review,
cancellation epochs, holds, and quarantine. Swift owns only revision-bound controls
and projection. Playwright and cross-browser testing do not apply to this native
Swift/Rust/process surface.

The disposable Mac process harness reports 21 proof keys using real authenticated
HTTP, SQLite, filesystem changes, and validation subprocesses with deterministic
model and reviewer fixtures. It exercises exactly 100 automatic model calls at the
absolute cap and observes no 101st call. It also exercises the project-rename
cleanup probe cross-platform. Actual native Windows execution is required to
establish Windows directory-handle release and the Windows-only
delayed-descendant sentinel that the kill-on-close Job must reap. Its
shared-limit scenario uses the public
authenticated routes the Mac app uses: project chat plus an owner-approved manual
escalation consumes the one shared per-feature cap, a later automatic opportunity
stops at `limit_reached` at that cap without any repair-model or reviewer call,
the owner corrects the project bytes directly, and the validation-only Resume
proceeds without AI through fresh validation and one fresh independent review that
succeed. The harness does not establish live model quality, account entitlement,
Windows-native or installed-app behavior, rendered visual placement, hosted
checks, live-device QA, or production readiness. It is registered as the twelfth
script in `developer_workflow_e2e`, which runs through Cargo workspace testing on
both required hosts.

Native Windows execution of this harness, installed Developer app behavior,
rendered layout and keyboard navigation, the final full local gate, exact-commit
hosted checks, publication, signing, notarization, live-device QA, and production
readiness are not claimed by this repository evidence and remain separately
reportable proof layers. The handle-release and delayed-descendant Windows proof
stays pending until the native Windows run of this harness completes. Mac
fixture success and non-Windows/generic hosted checks are not substitutes; an
actual Windows-hosted native run may establish hosted Windows evidence, but it
is not live Windows deployment proof.
